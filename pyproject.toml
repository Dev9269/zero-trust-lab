[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "zero-trust-lab"
description = "CISA Zero Trust Maturity Model self-hosted lab"
requires-python = ">=3.11"
